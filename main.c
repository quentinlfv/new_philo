#include "philo.h"

int main(int argc, char **argv)
{
    t_philo *p;
    t_data  data;

    data = init_data(argc, argv, data);
    /* check arg */
    /* check if must eat is set*/
    /* init */
    /* launch */
    return (0);
}